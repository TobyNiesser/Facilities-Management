@page "{id:int?}"
@model FACILITIES.Pages.Frequencies.DetailsModel

@{
    ViewData["Title"] = "Details";
}

    <div class="kt-container kt-container--fluid pb-4" style="background-color: #fff;">
        <br />
        <div class="pt-3 pb-3 pl-3 pr-3">
            <h1><b>Frequency</b></h1>
            <h4>Details</h4>
        </div>
        <br />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Frequency.FrequencyAmount)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Frequency.FrequencyAmount)
            </dd>
        </dl>
    </div>

<div>
    <a class="btn btn-sm btn-primary" asp-page="./Edit" asp-route-id="@Model.Frequency.FrequencyID">Edit</a>
    <a class="btn btn-sm btn-primary" asp-page="./Index">Back to List</a>
</div>
