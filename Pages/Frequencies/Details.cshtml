@page
@model FACILITIES.Pages.Frequencies.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Frequency</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Frequency.FrequencyAmount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Frequency.FrequencyAmount)
        </dd>
    </dl>
</div>
<div>
    <a class="btn-sm btn-primary" asp-page="./Edit" asp-route-id="@Model.Frequency.FrequencyID">Edit</a>
    <a class="btn-sm btn-primary" asp-page="./Index">Back to List</a>
</div>
