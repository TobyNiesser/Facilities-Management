@page
@model FACILITIES.Pages.Frequencies.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Frequency[0].FrequencyAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Frequency) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FrequencyAmount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FrequencyID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FrequencyID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FrequencyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
