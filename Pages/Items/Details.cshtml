@page "{id:int?}"
@model FACILITIES.Pages.Items.DetailsModel

@{
    ViewData["Title"] = "Details";
}

    <div class="kt-container kt-container--fluid pb-4" style="background-color: #fff;">
        <br />
        <div class="pt-3 pb-3 pl-3 pr-3">
            <h1><b>System</b></h1>
            <h4>Details</h4>
        </div>
        <br />
        <div>
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Item.ItemName)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Item.ItemName)
                </dd>
            </dl>
        </div>
        <div>
            <a class="btn btn-lg btn-success" asp-page="./Edit" asp-route-id="@Model.Item.ItemID">Edit</a>
            <a class="btn btn-lg btn-primary" asp-page="./Index">Back to List</a>
        </div>
    </div>