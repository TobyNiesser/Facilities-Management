@page
@model FACILITIES.Pages.Permissions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Permissions</h1>

<p>
    <a asp-page="Create" style="color: #0275d8;" class="btn border border-primary"><span class="glyphicon glyphicon-plus" style="color: #0275d8;"></span> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Permission[0].Name)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Permission[0].AccountType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Permission) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.AccountType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PermissionID"><span class="btn glyphicon glyphicon-cog" style="color: #0275d8;"></span></a>
                <a asp-page="./Details" asp-route-id="@item.PermissionID"><span class="btn glyphicon glyphicon-th-list" style="color: #0275d8;"></span></a>
                <a asp-page="./Delete" asp-route-id="@item.PermissionID"><span class="btn glyphicon glyphicon-remove" style="color: #d9534f;"></span></a>
            </td>
        </tr>
}
    </tbody>
</table>
