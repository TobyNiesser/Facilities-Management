@page
@model FACILITIES.Pages.Responsibilities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Responsibility[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Responsibility[0].Responsible)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Responsibility) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Responsible)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ResponsibilityID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ResponsibilityID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ResponsibilityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
