@page
@model FACILITIES.Pages.Responsibilities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Responsibility[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Responsibility) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a class="btn-sm btn-primary" asp-page="./Edit" asp-route-id="@item.ResponsibilityID">Edit</a>
                <a class="btn-sm btn-primary" asp-page="./Details" asp-route-id="@item.ResponsibilityID">Details</a>
                <a class="btn-sm btn-danger" asp-page="./Delete" asp-route-id="@item.ResponsibilityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
