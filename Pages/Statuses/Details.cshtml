@page "{id:int?}"
@model FACILITIES.Pages.Statuses.DetailsModel

@{
    ViewData["Title"] = "Details";
}

    <div class="kt-container kt-container--fluid pb-4" style="background-color: #fff;">
        <br />
        <div>
            <h1><b>Statuses</b></h1>
            <h4>Details</h4>
        </div>
        <br />
        <div>
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Status.StatusIndicator)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Status.StatusIndicator)
                </dd>
            </dl>
        </div>
        <div>
            <a class="btn btn-sm btn-primary" asp-page="./Edit" asp-route-id="@Model.Status.StatusID">Edit</a>
            <a class="btn btn-sm btn-primary" asp-page="./Index">Back to List</a>
        </div>
    </div>

