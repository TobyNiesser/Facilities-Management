@page
@model FACILITIES.Pages.Suppliers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Supplier[0].SupplierOptions)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Supplier) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SupplierOptions)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SupplierID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SupplierID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SupplierID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
